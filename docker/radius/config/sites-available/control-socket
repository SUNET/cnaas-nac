# -*- text -*-
listen {
        type = control
	socket = ${run_dir}/${name}.sock
#       peercred = no
#       socket = ${run_dir}/control/${name}.sock
#       uid = freerad
#       gid = freerad
        mode = rw
}